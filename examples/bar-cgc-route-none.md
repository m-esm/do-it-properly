# Bar: legal CGC-SKIP route=none

`done = legal CGC-SKIP with route=none plus evidence=, proven by the skip line; not required: a pack`

This skip is legal. `route=` and `evidence=` are present. `route=none` is an allowed value.

```
## Structural discovery
CGC-SKIP: no code graph, markdown prose; route=none; evidence=characterization-test
```

`dispatch` accepts `route=ast-grep|rg|none`. The illegal twin (route=none, evidence missing) is `examples/bar-cgc-omit-evidence.md`.
