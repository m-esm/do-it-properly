# Bar: legal CGC-SKIP plus BRIEF.md-in-worktree

`done = legal CGC-SKIP line plus BRIEF.md-in-worktree, proven by the example file; not required: dispatch`

Source: live SSA `dispatch` structural gate (`route=` and `evidence=` required) plus Claude sandbox: `$DIR/brief.md` is unreadable, so copy into the worktree as `BRIEF.md` (or use grok/codex).

```
## Structural discovery
CGC-SKIP: docs-only, no code graph; route=ast-grep; evidence=ast-grep -p 'CGC-SKIP:' docs/
BRIEF.md in worktree (copy of $DIR/brief.md)
```

A skip line without `route=ast-grep|rg|none` and `evidence=` is not this bar. Dispatch itself is out of bar.
